# C++ Standard Template Library (STL) Cheatsheet

> **Overview**: Comprehensive cheat-sheet summarizing C++17 STL containers (`std::vector`, `std::list`, `std::deque`, `std::stack`, `std::queue`, `std::priority_queue`, `std::unordered_map`, `std::unordered_set`, `std::map`, `std::set`), iterators, algorithms (`std::sort`, `std::lower_bound`, `std::upper_bound`), and custom comparators. Full content will be populated during finalization as curriculum phases are generated.

---

## 1. Sequence Containers Summary
*(Content to be populated during finalization)*

## 2. Container Adapters & Priority Queues
*(Content to be populated during finalization)*

## 3. Associative & Unordered Containers
*(Content to be populated during finalization)*

## 4. Key STL Algorithms & Syntax Guide
*(Content to be populated during finalization)*
