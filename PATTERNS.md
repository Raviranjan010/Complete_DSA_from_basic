# Master Algorithmic Patterns Index (`PATTERNS.md`)

This file contains the **Controlled Vocabulary of Pattern Tags** used across all practice files in the `DSA-Zero-To-Hero` repository. Each tag indexes practice questions that require or highlight that specific pattern.

---

## 🧭 Controlled Pattern Vocabulary & Question Index

### 1. Array & String Patterns

#### `#two-pointers`
*Opposite-direction or same-direction dual pointer traversal (e.g., pair sum, palindrome check, Dutch national flag).*
*(Questions will be linked as practice files are generated)*

#### `#sliding-window`
*Fixed or variable size window over contiguous subarrays or substrings.*
*(Questions will be linked as practice files are generated)*

#### `#prefix-sum`
*Cumulative sum array for $O(1)$ range sum queries and difference array updates.*
*(Questions will be linked as practice files are generated)*

#### `#kadane`
*Dynamic maximum subarray sum optimization in linear time.*
*(Questions will be linked as practice files are generated)*

#### `#binary-search`
*Search space halving on sorted arrays, rotated arrays, or monotonic answer spaces.*
*(Questions will be linked as practice files are generated)*

---

### 2. Stack, Queue & Linked List Patterns

#### `#fast-slow-pointers`
*Floyd’s tortoise and hare technique for cycle detection and middle-node identification.*
*(Questions will be linked as practice files are generated)*

#### `#monotonic-stack`
*Maintaining increasing or decreasing stack ordering for next greater/smaller element problems.*
*(Questions will be linked as practice files are generated)*

#### `#monotonic-queue`
*Maintaining ordered double-ended queue for sliding window maximum/minimum.*
*(Questions will be linked as practice files are generated)*

---

### 3. Hashing Patterns

#### `#hashing-frequency`
*Frequency counting and hash map lookups on arrays and strings.*
*(Questions will be linked as practice files are generated)*

#### `#hashing-two-sum`
*Complement lookup using hash tables for $O(n)$ target sum identification.*
*(Questions will be linked as practice files are generated)*

#### `#prefix-sum-hashing`
*Combining cumulative sums with hash maps to find subarrays with specific properties.*
*(Questions will be linked as practice files are generated)*

---

### 4. Recursion & Backtracking Patterns

#### `#backtracking`
*State space search with choose $\to$ explore $\to$ un-choose recursion (subsets, permutations, N-Queens, grid mazes).*
*(Questions will be linked as practice files are generated)*

---

### 5. Tree & BST Patterns

#### `#tree-dfs`
*Recursive or iterative Preorder, Inorder, and Postorder tree traversals.*
*(Questions will be linked as practice files are generated)*

#### `#tree-bfs`
*Level-order traversal and tree boundary/view computations using a queue.*
*(Questions will be linked as practice files are generated)*

#### `#bst-property`
*Leveraging sorted Inorder traversal and node constraints in Binary Search Trees.*
*(Questions will be linked as practice files are generated)*

#### `#trie-prefix`
*Prefix tree structure for autocomplete, dictionary lookups, and prefix matching.*
*(Questions will be linked as practice files are generated)*

#### `#segment-tree`
*Tree-based range query structure supporting $O(\log n)$ updates and range aggregations.*
*(Questions will be linked as practice files are generated)*

#### `#fenwick-tree`
*Binary Indexed Tree for efficient prefix sums and point updates.*
*(Questions will be linked as practice files are generated)*

---

### 6. Heap & Priority Queue Patterns

#### `#two-heaps`
*Maintaining min-heap and max-heap concurrently for dynamic median tracking.*
*(Questions will be linked as practice files are generated)*

#### `#k-way-merge`
*Priority-queue driven merging of $k$ sorted arrays or linked lists.*
*(Questions will be linked as practice files are generated)*

#### `#top-k-elements`
*Min-heap or max-heap filtering for $k$ largest/smallest/most frequent elements.*
*(Questions will be linked as practice files are generated)*

---

### 7. Graph Patterns

#### `#graph-bfs`
*Breadth-First Search for shortest paths in unweighted graphs and multi-source propagation.*
*(Questions will be linked as practice files are generated)*

#### `#graph-dfs`
*Depth-First Search for connected components, cycle detection, and path finding.*
*(Questions will be linked as practice files are generated)*

#### `#topological-sort`
*DAG node ordering using Kahn's BFS algorithm or DFS post-order stack.*
*(Questions will be linked as practice files are generated)*

#### `#dijkstra`
*Greedy priority-queue search for single-source shortest path with non-negative weights.*
*(Questions will be linked as practice files are generated)*

#### `#bellman-ford`
*Single-source shortest path algorithm handling negative edge weights and negative cycles.*
*(Questions will be linked as practice files are generated)*

#### `#floyd-warshall`
*All-pairs shortest path matrix dynamic programming.*
*(Questions will be linked as practice files are generated)*

#### `#union-find`
*Disjoint Set Union (DSU) with path compression and rank optimization for connectivity.*
*(Questions will be linked as practice files are generated)*

#### `#minimum-spanning-tree`
*Prim's and Kruskal's greedy algorithms for connecting graphs with minimum total edge weight.*
*(Questions will be linked as practice files are generated)*

#### `#strongly-connected-components`
*Kosaraju's and Tarjan's algorithms for identifying maximal strongly connected subgraphs.*
*(Questions will be linked as practice files are generated)*

#### `#bipartite-graph`
*Graph 2-coloring check using BFS or DFS traversal.*
*(Questions will be linked as practice files are generated)*

---

### 8. Greedy Patterns

#### `#greedy`
*Local optimal choice strategy backed by greedy-choice property and exchange argument proofs.*
*(Questions will be linked as practice files are generated)*

---

### 9. Dynamic Programming Patterns

#### `#dp-1d`
*1-Dimensional linear state transitions (climbing stairs, house robber).*
*(Questions will be linked as practice files are generated)*

#### `#dp-2d-grid`
*2-Dimensional grid path counting and minimum cost traversals.*
*(Questions will be linked as practice files are generated)*

#### `#dp-knapsack`
*0/1 Knapsack, Unbounded Knapsack, and Subset Sum decision choices.*
*(Questions will be linked as practice files are generated)*

#### `#dp-lcs`
*Longest Common Subsequence state comparisons on string pairs.*
*(Questions will be linked as practice files are generated)*

#### `#dp-lis`
*Longest Increasing Subsequence dynamic state updates ($O(n^2)$ and $O(n \log n)$ binary search variant).*
*(Questions will be linked as practice files are generated)*

#### `#dp-mcm`
*Matrix Chain Multiplication and interval dynamic programming.*
*(Questions will be linked as practice files are generated)*

#### `#dp-tree`
*Tree-structured state evaluation (tree diameter, subtree path maximums).*
*(Questions will be linked as practice files are generated)*

#### `#dp-bitmask`
*State representation using bitmasks for TSP and assignment problems.*
*(Questions will be linked as practice files are generated)*

#### `#dp-digit`
*Position-by-position digit constraint dynamic programming.*
*(Questions will be linked as practice files are generated)*

#### `#dp-space-optimization`
*State dimension reduction using rolling arrays.*
*(Questions will be linked as practice files are generated)*

---

### 10. Advanced String Patterns

#### `#kmp`
*Knuth-Morris-Pratt string matching using Longest Prefix Suffix (LPS) array.*
*(Questions will be linked as practice files are generated)*

#### `#z-algorithm`
*Linear-time string pattern search using Z-box matches.*
*(Questions will be linked as practice files are generated)*

#### `#rabin-karp`
*Rolling hash computation for fast string search and string equality checks.*
*(Questions will be linked as practice files are generated)*

#### `#manacher`
*Linear-time longest palindromic substring discovery.*
*(Questions will be linked as practice files are generated)*

---

### 11. Bit Manipulation & Math Patterns

#### `#bit-manipulation`
*XOR cancellation, bit mask flags, set bit counting, and binary shifts.*
*(Questions will be linked as practice files are generated)*

#### `#number-theory`
*Sieve of Eratosthenes, Euclidean GCD/LCM, modular exponentiation, and modular inverse.*
*(Questions will be linked as practice files are generated)*
